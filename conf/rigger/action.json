{
    "mkdir": [
        {
            "dir": "${PRJ_HOME}/tmp",
            "mask": "777",
            "sudo": false
        },
        {
            "dir": "${PRJ_HOME}/logs",
            "mask": "777",
            "sudo": false
        }
    ],
    "exec": [
        "cd ${PRJ_HOME}/src/${PRJ_NAME};./dep.sh ensure",
        "${NGX_EXEC_PREFIX} -s reload"
    ]
}
