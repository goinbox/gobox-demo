{
    "is_dev": true,
    "idc": "dev",

    "log": {
        "level": 7
    },

    "pprof": {
        "online_enable_host": "${HOSTNAME}",
        "port": "${PPROF_PORT}"
    },

    "api_http": {
        "gohttp_port": "${API_GOHTTP_PORT}"
    },

    "redis": [
        {
            "host": "127.0.0.1",
            "pass": "123",
            "port": "6379"
        }
    ],

    "mysql": {
        "host": "127.0.0.1",
        "user": "root",
        "pass": "123",
        "port": "3306"
    }
}
